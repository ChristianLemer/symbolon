# Concepts

The "why" behind what you see in the dashboard. Optional reading — the dashboard works fine without it. But if you've ever wondered what a "cache hit" actually saves you, or why this dashboard's totals are lower than naively summing the JSONL files, this is where the answers live.

| Page | What it explains |
|---|---|
| [Tokens](tokens.md) | What input, output, and cache tokens are, in plain English. Why caching matters. |
| [Dedup and accuracy](dedup-and-accuracy.md) | How streaming snapshots work, and why this dashboard's totals match what the API actually billed. |

> **More to come.** As topics come up that deserve their own page (sessions and turns, the pricing plans, what subagents are), they'll land here. Suggestions welcome.
